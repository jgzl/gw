spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.dev.cn:8848
        namespace: gw-uat
      config:
        server-addr: nacos.dev.cn:8848
        namespace: gw-uat
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}