spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.dev.cn:80
        namespace: gw-dev
      config:
        server-addr: nacos.dev.cn:80
        namespace: gw-dev
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}