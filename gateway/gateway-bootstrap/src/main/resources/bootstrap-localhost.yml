spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos-localhost.152.70.108.210.nip.io:8848
        namespace: gw-localhost
      config:
        server-addr: nacos-localhost.152.70.108.210.nip.io:8848
        namespace: gw-localhost
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}