spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: gw-prod
      config:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: gw-prod
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}