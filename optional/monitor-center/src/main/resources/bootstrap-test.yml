spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: gw-test
      config:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: gw-test
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}